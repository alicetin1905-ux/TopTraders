node_modules/
.DS_Store
*.log
.env
.cache/
tmp/

# Built by the refresh pipeline and handed straight to Pages. Committing these
# grew the repo ~72KB per refresh (~315MB/year at the observed cadence); the
# published site carries the rolling state instead.
docs/data/*.json
